#include <stdio.h>
#include <math.h>

void calculateGoldenRatio(double *result) {
    *result = (1 + sqrt(5)) / 2;
}

int main() {
    double goldenRatio;

    calculateGoldenRatio(&goldenRatio);
    printf("The golden ratio is approximately: %lf\n", goldenRatio);

    return 0;
}
